# AGENTS.md — Vosmenog

> Фреймворк дисциплинированного агента. Это мета-проект: инструкции, метод, память.
> Репозиторий содержит обезличенный метод работы — без серверных конфигов,
> секретов и проектной специфики конкретной установки.

---

## Что здесь

| Что | Где |
|-----|-----|
| Репозиторий | клон `git@github.com:Skunokil/vosmenog.git` |
| Планирование и документы | `team-work/<project>/` (вне репозитория) |
| Метод (всегда в контексте) | `METHOD.md` |
| Шаблоны | `EPIC.template.md`, `TASK.template.md`, `BUG.template.md` |

---

## Git

```bash
git remote -v  # origin (SSH)
git branch     # main (единственная ветка, проект методологический)
```

- Коммиты и push — через владельца репозитория
- В репозитории — только обезличенный метод

---

## Структура репозитория

```
vosmenog/
  METHOD.md           # Ядро метода (контракт исполнителя)
  AGENTS.md           # Этот файл — проектные слоты
  EPIC.template.md    # Шаблон эпика
  TASK.template.md    # Шаблон задачи
  BUG.template.md     # Шаблон бага
  GUIDE.md            # Руководство по установке
  setup.sh            # Скрипт установки
  update.sh           # Скрипт обновления
  persona_vosya.md    # Персона агента-исполнителя
  payload/            # Установочные файлы
```

---

## Гейты — что требует разрешения владельца

| Действие | Кто решает |
|----------|-----------|
| Правка METHOD.md | Только владелец |
| Правка AGENTS.md | Только владелец |
| Правка шаблонов EPIC/TASK/BUG | Только владелец |
| Правка opencode.json | Только владелец |
| Правка STARTUP.md | Только владелец |
| Файлы в ~/agent-os/ | Только владелец |
| Push на GitHub | Только владелец |
| Чтение любых файлов | Агент (без gate) |
| Создание/обновление EPIC в team-work | Агент (после go) |
| Создание TASK/BUG | Агент (в рамках эпика) |
| Resolve эпика | Агент (с ретроспективой) |

---

## Как улучшать агента

- Улучшение агента = правка **дистрибутива** (`payload/` в этом репозитории).
  Живые файлы на сервере (`~/agent-os/`, `~/.config/opencode/`) — результат
  раскатки, правятся через дистрибутив.
- Обновление агента — через раскатку дистрибутива (владелец: `cp`/setup/update).
- Навыки/скиллы — улучшаются по итогам ретро в процессе работы, обычным циклом.

---

## Границы репозитория

- **В репозиторий** — только обезличенный метод работы: METHOD.md, шаблоны,
  AGENTS.md, setup/update скрипты. Никаких серверных конфигов, секретов,
  проектной специфики конкретной установки.
- **Вне репозитория** — серверные конфиги (CLAUDE.md, opencode.json, .bashrc),
  STARTUP.md, team-work/ документы. Они живут на сервере, правятся отдельно.
- **Общие практики** — выносятся в репозиторий (METHOD.md).
- **Локальные процедуры** — остаются в team-work/ и серверных конфигах.
