# TASK-<id>: <короткое имя>

> Атомарный шаг внутри эпика. Развилки УЖЕ разрешены в EPIC — агент исполняет,
> не проектирует. Удали этот блок в готовой задаче.

**id:** TASK-<NNN> · **Эпик:** EPIC-<NNN> · **Проект:** `<путь>` · **Ветка:** `<…>` · **Effort:** Max

---

## 1. Цель

Одно-два предложения: что станет правдой. Без «как».

## 2. AC этой задачи

Подмножество AC эпика, которое закрывает именно эта задача:

- <проверяемый критерий>

## 3. Контекст (адреса, не «найди»)

- `путь/к/файлу.ext:NN` — что здесь сейчас, что делаем
- `путь/к/другому.ext:NN–MM` — …

Перед правкой — рекон каждого адреса (`grep -n`/`cat`), подтвердить строку.

## 4. Развилки — разрешены в EPIC (не пересматривать)

Ссылка на Decisions эпика. Встретил выбор, которого там нет → стоп, эскалируй.

## 5. Шаги (атомарные, по одному)

1. <шаг> → верификация: `<команда>` показывает `<что>`
2. …

Между шагами — вывод владельцу, ждать подтверждения.

## 6. Слоты-команды проекта

Из `<проект>/AGENTS.md`. Сюда — только отличия от дефолта:

- Патч: `<…>` · Билд: `<…>` · Миграция: `<…>`

## 7. Чек-лист done

- [ ] Адреса §3 проверены реконом до правки
- [ ] Каждый файл верифицирован (`grep`/`repr`)
- [ ] Билд/линт без ошибок
- [ ] Нетривиальное? → code review Восе перед коммитом
- [ ] Визуальная проверка владельцем — ДО коммита
- [ ] Гранулярные коммиты после визуалки
- [ ] Статус в EPIC обновлён

## 8. Что НЕ делать

- Не генерировать/собирать до go.
- Не решать развилки вне Decisions эпика.
- Не трогать файлы вне §3 без согласования.
- Не `sed` по кириллице.
- Не коммитить до визуального подтверждения.
