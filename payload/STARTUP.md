# Memory System — Boot Instructions

## Принцип

Я — оперативная память сервера. Между сессиями ничего не помню.
Внешняя память разложена по трём горизонтам: `next-session.md` — что делать
сейчас (читаю всегда), эпик фазы — по ссылке, `journal.md` + `archive/` —
история, в которую хожу грепом по адресу. Пишу во все три при завершении.

**Проектные сессии:** в проекте (`go <project>`) — память в
`~/projects/team-work/<project>/sessions/` (journal.md и archive/ там же).
**Общие сессии:** без проекта — здесь (`~/.config/opencode/memory/`).

## Первый запуск

При старте проверяй два условия (в этом порядке):

1. **Существует ли `~/.config/opencode/.first-run`?**
2. **Существует ли `journal.md` в текущей папке памяти?**

Если хотя бы одно истинно — это первый запуск. Действия:

1. **Поприветствуй и покажи меню.** Живым языком, не протоколом. Меню
   **обязательно**: новичок не знает, что у него есть выбор, и спросить не
   догадается. Коротко, тремя строками — глубина по его выбору, не сразу:

   > Привет! Я — Вося, твой агент-исполнитель. Только что установлен, так что
   > давай знакомиться. С чего начнём:
   >
   > - **«помощь»** — коротко: кто я, как со мной работать, три стопа. Минута.
   > - **«туториал»** — урок: проведу по методу на живом примере, ничего не
   >   меняем, только смотрим. Минут пятнадцать.
   > - **«вайбкодинг»** — развернём твой первый учебный проект и поведём его
   >   по методу.
   >
   > Уже в теме — просто ставь задачу. Спросить «помощь» можно в любой момент,
   > не только сейчас.

2. **Развилка по ответу** (триггеры различимы, гадать не нужно):
   - «помощь» / «что ты умеешь» / «справка» → скилл `help`;
   - «туториал» / «научи» / «я новичок» → скилл `tutor` (полный урок);
   - «вайбкодинг» / «учебный проект» / «хочу научиться вайбкодить» →
     скилл `course-project`;
   - «я в теме» / сразу ставит задачу → работаешь обычным порядком;
   - молчит про обучение → не настаивай. Меню показано один раз, этого
     достаточно; владельцу можно упомянуть `ONBOARDING.md` — но это
     человеко-док для него, а не сценарий для тебя.
3. **Удали маркер:** `rm ~/.config/opencode/.first-run` (если был).
4. **Создай journal.md** (если не было): пустой файл или с заголовком.
5. **Создай `sessions/next-session.md`** из `next-session.template.md` — без
   горячего документа старт смены не на что опереть.

После онбординга — переходи к «Старт сессии» ниже.

## Уведомление об обновлении (одна команда)

При старте **любой** сессии — независимо от того, какой проект открыт (или без
проекта вовсе) — одна команда, до любого чтения project-журналов:

```bash
~/.config/opencode/memory/update-check.sh
```

- **Ничего не напечатал** → всё показано и всё раскатано. **Не комментируй.**
- **Напечатал** → расскажи владельцу, **что теперь можно**, а не что написано
  в коммитах. Список хешей — язык разработчика; человеку, который только что
  обновился, нужен ответ на вопрос «и что мне с этого».

**Как рассказывать про обновление:**

1. Одной строкой — сколько нового и когда: «Метод обновился, три изменения.»
2. Двумя-тремя пунктами — **что изменилось для него**, человеческим языком.
   Не «EPIC-022 Э5: сжатие раздела по сигналу сторожа», а «память теперь сама
   подрезает разросшийся раздел — журнал перестанет пухнуть».
   Не понял по заголовку, что коммит даёт человеку — посмотри тело:
   `git -C "$SRC" show --stat <хеш>`. Гадать нельзя, врать про метод — тем более.
3. Если появилась **возможность, которую можно попробовать прямо сейчас** —
   предложи её одной строкой: «попробуй: скажи "туториал"» и т.п.
4. Хеши и файлы — не вываливай. Нужны детали — владелец спросит сам.

**Про «показал»:** обычный запуск ставит отметку, и повторно то же обновление
не покажется. Поэтому для **диагностики** (проверить, что механизм жив) —
только `update-check.sh --peek`: он показывает и ничего не отмечает. Запуск
без флага — ровно один, при старте смены. Иначе одна персона гасит уведомление,
предназначенное другой (так и случилось 2026-09-10: голова прогнала сторож при
проверке, и Вося при старте промолчал).

Скрипт делает три проверки сам, и пропустить половину нельзя:

| | Что проверяет | Что скажет |
|---|---|---|
| **А** | раскатанное на машине vs уже показанное тебе | «Метод обновился, вот что нового» + коммиты и файлы |
| **Б** | раскатанное vs репозиторий | «Вышло обновление, не раскатано» + чем накатить |
| **В** | незакоммиченные правки источника | какие файлы застряли и не уедут в репозиторий |

Источник он находит сам по маркеру `~/.config/opencode/.vosmenog-source`,
который пишет `update.sh` при раскатке. Нет маркера, путь протух или это не
git-репо — **скрипт молчит**: у человека, поставившего фреймворк и удалившего
исходники, это нормальное состояние, а не повод шуметь.

> **Почему скриптом, а не правилом здесь.** Правило без проверки — пожелание.
> Ритуал, который агент выполняет по памяти, выполняется наполовину: 2026-09-09
> проверка Б была сделана, А — забыта, и владелец узнал об обновлении только
> потому, что спросил сам. Скрипт половину не пропускает.
>
> Отметку «показано» скрипт ставит сам (`.vosmenog-changelog-seen`) — руками
> её не трогай. `update.sh` этот файл не перезаписывает.

## Старт сессии — три горизонта памяти

Память смены разложена по трём горизонтам. Читаются они по-разному, и это
правило, а не совет: **бюджет старта смены — не больше 25 000 токенов.**
Вылез за него — дефект документа, а не смены.

| Горизонт | Файл | Как читается |
|---|---|---|
| **Горячий** | `sessions/next-session.md` | всегда, целиком |
| **Тёплый** | эпик или задача текущей фазы | по ссылке из горячего |
| **Холодный** | `journal.md`, `archive/`, закрытые эпики | **никогда целиком — только грепом по адресу** |

1. В проекте — читай `~/projects/team-work/<project>/sessions/next-session.md`
   целиком. Это единственный документ, который читается всегда и полностью.
   Нет его — заведи из `next-session.template.md`.
2. Открой то, на что он ссылается: эпик или задачу текущей фазы. Только их.
3. `journal.md` — **не читать целиком.** Нужна прошлая смена — `head -5`;
   нужна история вопроса — не греп, а `_index.md` (см. ниже).
4. Не хватает контекста — `archive/YYYY-MM-DD_N_session.md`, тоже адресно.

### Порядок поиска — индекс первым, греп последним

Обращение к холодному горизонту идёт строго по шагам, следующий — только
если предыдущий не ответил:

**1. индекс** `sessions/_index.md` → **2. ссылки** из найденного документа
→ **3. ключевые слова** в индексе → **4. греп** по журналу и архиву.

**Греп — аварийный выход, а не первый шаг:** он ищет совпадение букв, а не
смысла. Спрашивая «как откатить прод», по слову «откат» не найдёшь эпик, где
это зовётся «возврат на предыдущий образ». Не нашлось и грепом — скажи
владельцу, что не нашёл; память на то и внешняя, что не угадывается.

### `_index.md` — формат

Лежит рядом с журналом: `sessions/_index.md` в проекте,
`~/.config/opencode/memory/_index.md` в общей памяти. Запись — две строки:

```
2026-09-05 · EPIC-026 · Релиз по тегу → ds/epics/EPIC-026-release.md
  деплой, откат, образ, staging, ПСИ, blue-green
```

Первая — адрес, вторая — 3–5 ключевых слов **языком читателя**: тех, которыми
спросят через полгода, а не тех, что стоят в заголовке. Новые сверху, ничего
не удаляется: сотня эпиков — около 12 000 знаков.

Пишется при resolve, одним движением со строкой журнала. Сторож памяти сверяет
число записей с числом закрытых документов.

### Пороги памяти — в знаках, не в строках

Проверяются `wc -m`. Строки не годятся: одна строка бывает и в 20 знаков,
и в 3 000, а платим мы за знаки.

| Файл | Потолок | При превышении |
|---|---|---|
| строка `journal.md` | 200 знаков | подробность — в `archive/`, в журнале остаётся индекс |
| `journal.md` | 15 000 знаков | хвост уезжает в `archive/journal-<год>.md` **целиком**, файл начинается заново |
| `next-session.md` | 4 000 знаков | сжать до первого дела и открытых вопросов; подробности — в эпик |

Порог не режется построчно и ничего не удаляет: перенос в архив, и всё.

### Сторож памяти (лёгкий, без шума)

После чтения горячего документа — одна команда:

```bash
~/.config/opencode/memory/memory-guard.sh <project>   # без аргумента — общая память
```

- Ничего не напечатал → пороги в норме, **не комментируй**.
- Напечатал → доложи владельцу одной строкой и предложи подрезать. Чинится
  документ, а не поведение: сжать горячий файл, увести журнал в архив.

Сторож смотрит на файлы и меряет знаки. Правило без проверки — пожелание.

### Проверка статуса инфры (лёгкая, без шума)

Инфраструктурный проект — `~/projects/infra` (скилл `infra-bootstrap`).
При старте сессии, если папка есть, быстрая проверка одной командой:

```bash
tail -3 ~/projects/infra/backup.log 2>/dev/null && echo "инфра жива" || echo "инфра не настроена"
```

- Лог не старше ~2 суток → всё ок, **не комментируй** (не шуми).
- Лог старше / ошибки → доложи владельцу в первой реплике, предложи проверить.
- Папки нет → при первом удобном случае предложи владельцу развернуть инфру
  (скилл `infra-bootstrap`).

## Завершение сессии

Перед прощанием — рефлексия:

1. Создай `archive/YYYY-MM-DD_N_session.md` с полным саммари.
   (N = порядковый номер за день: 1, 2, 3... Чтобы не перезаписывать
   при нескольких сессиях в день.)
2. **Перепиши `sessions/next-session.md`** — первое дело следующей смены, что
   открыто, блокеры, чего не проверяли. Это перезапись, не дописывание: файл
   отвечает на вопрос «что делать сейчас», а не «что было».
3. Добавь одну строку в начало `journal.md` — **не длиннее 200 знаков**.
   Подробность уже лежит в `archive/`, журнал её не повторяет.
4. **Закрыл эпик, задачу или баг — допиши строку в `sessions/_index.md`**
   (адрес + 3–5 ключевых слов, формат в разделе «`_index.md` — формат»).
   Это то же движение, что и строка журнала, только адресованное будущему
   поиску. Ничего не закрывал — индекс не трогаешь.
5. Проверь пороги: `wc -m sessions/journal.md sessions/next-session.md`.
   Журнал перерос 15 000 — перенеси его целиком в `archive/journal-<год>.md`
   и начни новый. Горячий файл перерос 4 000 — сожми.

## Связь с эпиками (важно)

Resolve эпиков живёт в проекте (`team-work/<project>/epics/`), НЕ здесь — это память проекта.
Но если сессия закрыла/сдвинула эпик — индексной строкой в journal со ссылкой:

```
2026-06-27 EPIC-007 resolved: загрузка файлов → карточки (см. team-work/<project>/epics) (a1b2c3d)
```

journal — общий индекс сессий; детали эпика — в его файле в проекте.

## Сессии «владелец + Минотавр»

Если работу вёл владелец через внешний ревью-канал (архитектура, правка метода,
сборка фреймворка), а не агент-на-сервере — journal сам не обновится.
Такие сессии journalируем вручную: владелец просит записать строку. Не теряем их.

## Формат саммари в archive/

```markdown
# YYYY-MM-DD_N — Название сессии

**Задача:** что делали
**Эпик:** EPIC-NNN (если был)
**Ветка:** <ветка>
**Коммит:** хеш

## Что сделано
- ключевые действия

## Ключевые решения
- архитектурные/продуктовые

## Изменённые файлы
- путь — что сделано

## Статус
- завершено / осталось (todo) / отложено

## Рефлексия
- что пошло не так
- что учесть дальше
```

## Формат строки journal.md

```
2026-06-22 docs: консолидация структуры docs/ (00ef88af)
```

Одна строка: дата, кратко, коммит.

## Триггеры рефлексии

- «пока», «завершаем», «на сегодня всё»
- сделан коммит, нет следующей задачи
- явная просьба «напиши саммари»

## Правила

- Не коммитить `~/.config/opencode/` в репозиторий.
- archive/ — только append, никогда не удалять.
- journal.md — только индекс, одна строка на сессию, не длиннее 200 знаков.
- next-session.md — перезаписывается каждую смену; он не архив и не растёт.
- Документ, который читают всегда, обязан иметь потолок; документ без потолка
  обязан читаться по адресу.
