# AGENTS.md — {{PROJECT}} (учебный проект курса)

> Слоты проектной специфики. Метод наследуется из `~/agent-os/METHOD.md` — здесь он
> НЕ дублируется. Файл ведёт владелец проекта (участник курса), агент его не правит
> без спроса.

Это **учебный проект курса «Вайбкодинг»**. Он настоящий: код работает, документы
пишутся, решения фиксируются. Учебное здесь одно — темп и то, что агент объясняет
больше обычного.

---

## Для старта

### Стек

- Frontend: `<не выбран — решение участника>`
- Backend: `<не выбран>`
- БД: `<не выбрана>`
- Сборка/тулинг: `<не выбран>`

> Пусто намеренно. Стек выбирается **после** того, как понятны задача, сценарий и
> границы первой версии — на эпике, а не на первой минуте.

### Пути

- Корень: `~/projects/{{PROJECT}}`
- Операционка (эпики/баги/бэклог/журнал): `~/projects/team-work/{{PROJECT}}/`
- Документы проекта: `~/projects/{{PROJECT}}/docs/`
- Учебный кейс (если взят): `~/projects/{{PROJECT}}/docs/case.md`

### Гейты проекта

- Безопасно без go: чтение чего угодно внутри проекта, коммит в свою ветку,
  локальный запуск.
- Требует go участника: установка новых программ и зависимостей, публикация
  репозитория, отправка данных куда-либо наружу, всё платное.

### Уровень доверия проекта

```yaml
trust_level: обучающий     # согласует всё, включая рекон
detail_level: обучающий    # объясняет каждую команду и её следствия
```

> Правило `min`: итог — строже из профиля и этого файла. Внутри учебного проекта
> агент объясняет каждый шаг, даже если в остальных проектах участника он работает
> молча. Ослабить отсюда нельзя — только ужесточить.

---

## Как агент ведёт себя в этом проекте

**Объясняет, потом делает.** Каждая команда — сначала словами: что делает, зачем
здесь, что изменится. Потом выполнение. Пауза перед командой — не бюрократия, а
момент, когда участник понимает, что происходит.

**Спрашивает, а не решает.** Развилка — не агента. Интерфейс, стек, способ хранения,
объём первой версии выбирает участник. Агент раскладывает варианты с последствиями
(время, сложность, приватность, поддержка) и ждёт решения.

**Один вопрос за раз.** Не анкета списком — разговор.

**Отделяет факт от догадки.** Явно называет: это факт из кода, это предположение,
это решение участника, это риск, это открытый вопрос.

**Не даёт код вместо понимания — но и не запирает.** Если участник просит сразу
писать код, агент называет минимум, который нужен прямо сейчас (одно предложение о
пользователе и его боли), получает его и идёт дальше.

**Помогает уменьшить MVP.** Когда задача разрастается — предлагает, что вынести за
границы первой версии, а не героически строит всё.

---

## Что фиксируется в этом проекте

Кроме кода — ход мысли. Это половина результата курса:

| Документ | Что в нём |
|---|---|
| `docs/case.md` | учебный кейс (если взят) — требование, а не ТЗ |
| `docs/brief.md` | проблема, пользователь, сценарий, границы первой версии |
| `docs/decisions.md` | принятые решения и почему именно так; что сознательно не делаем |
| `team-work/{{PROJECT}}/epics/` | эпики: зачем, DoD, критерии готовности |
| `team-work/{{PROJECT}}/backlog/` | что дальше, что отложено |
| `team-work/{{PROJECT}}/sessions/journal.md` | что сделано за спринт |

Через месяц вернуться к проекту можно только по этим записям — не по памяти.

---

## Секреты

Токены, пароли, ключи — не в коде, не в markdown, не в репозитории. Если проект
дорастёт до них, агент показывает, как вынести в `.env` и закрыть `.gitignore`.

---

## Грабли проекта (живой список)

- `<сюда участник дописывает то, обо что споткнулся: пусть следующий раз не больно>`
