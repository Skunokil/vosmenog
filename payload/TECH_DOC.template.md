# <Название инструкции>

> Кратко: что решает эта инструкция и когда применять.
> Tech-doc — это знание из опыта, которое не влезает в METHOD.md, но критично
> для качества. Живёт в `tech-docs/` проекта.

**Домен:** <фронт / бэкенд / БД / инфра / метод / дизайн>
**Автор:** <кто написал>
**Дата:** <YYYY-MM-DD>
**Связанный эпик:** EPIC-<NNN> (если есть)

---

## Контекст

<Где и при каких условиях возникает эта проблема. Пример: «При реализации
выпадающего меню в React возникает проблема с overflow у родителя.»

Читатель должен понять: «а, это про我的 случай» — или «нет, это не про меня»
за 10 секунд.

---

## Проблема

<Что именно идёт не так. Конкретно, без воды.

Пример:
- dropdown обрезается overflow: hidden родителя
- z-index конфликтует с модалкой
- click-outside не работает с динамически созданными элементами>

---

## Решение

<Пошаговая инструкция что делать.

Пример:
1. Добавить `position: relative` на контейнер dropdown
2. Вынести dropdown в React Portal (render outside DOM tree)
3. Использовать `useRef` + `useEffect` для click-outside>

---

## Пример кода

```<язык>
// Конкретный пример решения — не абстрактный, а рабочий кусок
```

---

## Антипаттерны

<Чего НЕ делать — и почему.

Пример:
- ❌ Не использовать `overflow: visible` на родителе — ломает layout
- ❌ Не ставить z-index вручную — конфликтует, используй CSS-переменные
- ❌ Не обрабатывать click-outside через document.addEventListener без cleanup>

---

## Связанные инструкции

- [ссылка на другой tech-doc, если есть]

---

> Обновляется при遇到 новых кейсах. Не удаляй — дополняй.
