# AGENTS.md — <Имя проекта> (слоты проекта)

> **Слоты проектной специфики.** Копируется в `<проект>/AGENTS.md` (родной формат
> opencode — подхватывается автоматически, когда агент в папке проекта). Метод
> наследуется из `~/agent-os/METHOD.md` — здесь его НЕ дублировать. Заполни каждый
> слот; пустой = дыра. Файл ведёт ВЛАДЕЛЕЦ, агент его не редактирует.
> Удали этот блок в готовом файле.

---

## Стек

- Frontend: `<…>`
- Backend: `<… / нет>`
- БД: `<… / нет>`
- Сборка/тулинг: `<…>`

## Пути

- Корень: `<путь>`
- Фронт: `<путь>`
- Бэк: `<путь>`
- Билд (`dist/` и т.п.): `<путь>`
- Эпики/доки: `docs/epics/`, `docs/` <или свой путь>

## Git-состояние (проверить реконом ДО первого TASK)

- Репозиторий инициализирован? `git rev-parse --is-inside-work-tree`
- Remote: `<git@… / нет>`
- **Схема ветвления проекта:** `<например: main + feature-ветки>`
  — НЕ наследовать чужую доктрину (типа «только одна ветка X») вслепую. Объяви свою.

## Патч файлов (слот под «не sed, верифицируй якорь»)

```bash
python3 - << 'PYEOF'
path = '<файл>'
with open(path) as f: src = f.read()
old = '<точная строка>'
idx = src.find(old)
print('found at:', idx)          # -1 = не найдено, не писать
if idx >= 0:
    src = src.replace(old, '<новая строка>', 1)
    with open(path, 'w') as f: f.write(src)
    print('OK')
PYEOF
```

## Билд

```bash
<команда сборки>
```

## Линт / тесты

```bash
<линт>
<тесты / 'не настроены'>
```

## Миграции БД

```bash
<workflow / 'нет БД'>
```

## Контейнеры / сервисы

- `<имя>` — `<роль, порт>` <или 'нет'>

## Гейты проекта

- Безопасно без go: `<например: коммит в feature-ветку, dev-сборка>`
- Требует go: `<деплой, прод-сборка, миграции, новые зависимости>`

## Грабли проекта (живой список)

- `<стек-специфичная ловушка>`
